# theozolith-control

Control Node: fleet dashboard, heartbeat/command + run-event API, and janitor jobs
(zombie-claim sweeps, retry audits). See `docs/specs/NODE-SUBSTRATE.md`.

**Stub in M1** — the component lands in a later milestone.
