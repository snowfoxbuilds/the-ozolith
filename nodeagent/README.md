# theozolith-nodeagent

Node Agent: the uncontainerized TheOzolith daemon installed on every physical node,
registering it as a Container-Host — heartbeats, infrastructure-command reconciliation,
local derived-image builds, and docker stack lifecycle. See `docs/specs/NODE-SUBSTRATE.md`.

**Stub in M1** — the component lands in a later milestone.
